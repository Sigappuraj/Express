<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/teachers">Teachers</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item {{#if (ifEquality title "Teacher Student Details")}}active{{/if}}">
        <a class="nav-link" href="/teachers/:id/students">Students {{#if (ifEquality title "Student Details")}}<span
            class="sr-only">(current)</span>{{/if}}</a>
      </li>
      <li class="nav-item {{#if (ifEquality title "Add Student")}}active{{/if}}">
        <a class="nav-link" href="/add-student">Add Students {{#if (ifEquality title "Add Student")}}<span
            class="sr-only">(current)</span>{{/if}}</a>
      </li>
    </ul>
  </div>
</nav>